import { fireEvent, render, screen } from '@testing-library/react'; import { expect, test, vi } from 'vitest'; import { ShortenForm } from './ShortenForm';
test('submits a URL and displays the returned short link',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,status:201,json:()=>Promise.resolve({id:'1',short_url:'https://sho.rt/demo',short_code:'demo',original_url:'https://example.com',title:null,custom_alias:null,is_active:true,expires_at:null,created_at:'',updated_at:'',total_clicks:0})}));render(<ShortenForm/>);fireEvent.change(screen.getByLabelText('Long URL'),{target:{value:'https://example.com'}});fireEvent.click(screen.getByRole('button',{name:'Shorten link'}));expect(await screen.findByText('https://sho.rt/demo')).toBeInTheDocument();});
